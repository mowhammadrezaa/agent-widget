#!/bin/zsh
# Agent Widget — macOS uninstall entrypoint
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$ROOT"

if ! command -v node >/dev/null 2>&1; then
  echo "Node.js is required to run the uninstall wizard." >&2
  exit 1
fi

exec node "$ROOT/scripts/uninstall-wizard.cjs" "$@"
